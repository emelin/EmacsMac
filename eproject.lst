;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("pli" "/home/yukang/workspace/kanglocal/pli3/." "PLI.cfg")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 52)
        (left . 0)
        (width . 80)
        (height . 35)))

